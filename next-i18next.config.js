const path = require('path');

module.exports = {
  i18n: {
    locales: ['vi', 'en'],
    defaultLocale: 'en'
  }
  // localePath: path.resolve('src/translation'),
  // localeStructure: '{{lng}}'
  // localExtension: null
};
